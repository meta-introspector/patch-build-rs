[package]
name = "patch-build-rs-macros"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
syn = { workspace = true, features = ["full", "extra-traits", "visit-mut"] }
quote = { workspace = true }
proc-macro2 = { workspace = true }
darling = "0.20" # Not in workspace, keep explicit version
serde = { workspace = true }
anyhow = { workspace = true }

[features]
default = []
test_build = [] # For testing build.rs generation
